/* import * as d3 from "d3";
import database from "./data/eldercare.csv"

const API = {
    async getdata() {
        const dataConverted = d3.csv(database);
        const data = await dataConverted;
        return data;
      }
}
export default API; */